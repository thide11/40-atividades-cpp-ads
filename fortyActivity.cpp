#include "fortyActivity.h"
#include <iostream>
#include <sstream>

using namespace std;

void FortyActivity::run() {
  print("Nenhuma camada de implementacao foi requirida, verifique o codigo fonte do arquivo fortyActivity.h para ver a modelagem das classes");
};